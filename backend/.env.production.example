# Variables de Entorno para Railway/Render (Backend)
# Copia estos valores en el dashboard de Railway/Render

# PostgreSQL (Railway genera estos automáticamente)
# Solo necesitas configurar estos si usas Render u otro servicio
PGUSER=postgres
PGPASSWORD=<generado_automaticamente>
PGHOST=<generado_automaticamente>
PGPORT=5432
PGDATABASE=railway

# Puerto del servidor (Railway usa PORT automáticamente)
PORT=4000

# URL del frontend desplegado en Vercel
# Actualiza esto después de desplegar el frontend
FRONTEND_URL=https://tu-app.vercel.app
